#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createItemsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createItemsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Items $colStr VALUES $valStr", "SELECT * FROM Items WHERE id=$ctx.args.createItemsInput.id"]
}