#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateTablesInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateTablesInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE tables SET $update WHERE id=$ctx.args.updateTablesInput.id", "SELECT * FROM Tables WHERE id=$ctx.args.updateTablesInput.id"]
}